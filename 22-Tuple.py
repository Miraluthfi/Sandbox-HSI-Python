"""Tuple mirip list tetapi list [], tuple (). Jika di list bisa tambah hapus elemen,
tetapi di tuple tidak bisa diubah lagi (immutable)
"""
numbers = (5, 3, 1, 2, 4)
print(numbers[2])

# lebih prefer menggunakan list